/*
 * 'window_badge' component.
 *
 *  Display the specified text (usually a count) as a badge in the window titlebar.
 *
 *  Note that the whole stuff is managed by the IWindowed interface: just add the
 *  'ronin-iwm-titlebadge' here, and the DOM element will be moved accordingly.
 *
 *  Parameters:
 *  - text: the text to be displayed.
 */
import '/imports/client/interfaces/iwindowed/iwindowed.js';
import './window_badge.html';
